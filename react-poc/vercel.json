{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://YOUR-MAIL-BACKEND/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
